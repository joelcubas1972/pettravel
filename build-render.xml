<?xml version="1.0" encoding="UTF-8"?>
<project name="PetTravelDocs-Render" default="dist" basedir=".">

    <!-- ===========================
         PROPIEDADES DEL PROYECTO
         =========================== -->
    <property name="src.dir" value="src"/>
    <property name="web.dir" value="web"/>
    <property name="build.dir" value="build"/>
    <property name="build.classes.dir" value="${build.dir}/classes"/>
    <property name="build.web.dir" value="${build.dir}/web"/>
    <property name="dist.dir" value="dist"/>

    <!-- ===========================
         CLASSPATH - SOLO JARs LOCALES
         =========================== -->
    <path id="classpath">
        <fileset dir="${web.dir}/WEB-INF/lib" includes="**/*.jar" erroronmissingdir="false"/>
        <fileset dir="lib" includes="**/*.jar" erroronmissingdir="false"/>
    </path>

    <!-- ===========================
         CLEAN
         =========================== -->
    <target name="clean">
        <delete dir="${build.dir}"/>
        <delete dir="${dist.dir}"/>
        <mkdir dir="${build.dir}"/>
        <mkdir dir="${dist.dir}"/>
    </target>

    <!-- ===========================
         COMPILAR JAVA
         =========================== -->
    <target name="compile" depends="clean">
        <mkdir dir="${build.classes.dir}"/>
        <javac srcdir="${src.dir}" destdir="${build.classes.dir}"
               classpathref="classpath"
               includeantruntime="false"
               source="17" target="17"
               encoding="UTF-8">
            <compilerarg value="-parameters"/>
        </javac>
        <echo message="✅ Compilación completada"/>
    </target>

    <!-- ===========================
         COPIAR RECURSOS WEB
         =========================== -->
    <target name="copy-resources" depends="compile">
        <mkdir dir="${build.web.dir}"/>
        
        <!-- Copiar todos los archivos web -->
        <copy todir="${build.web.dir}">
            <fileset dir="${web.dir}"/>
        </copy>
        
        <!-- Copiar clases compiladas -->
        <copy todir="${build.web.dir}/WEB-INF/classes">
            <fileset dir="${build.classes.dir}"/>
        </copy>
        
        <!-- Copiar librerías -->
        <copy todir="${build.web.dir}/WEB-INF/lib">
            <fileset dir="${web.dir}/WEB-INF/lib" erroronmissingdir="false"/>
            <fileset dir="lib" erroronmissingdir="false"/>
        </copy>
        
        <echo message="✅ Recursos web copiados"/>
    </target>

    <!-- ===========================
         GENERAR WAR
         =========================== -->
    <target name="war" depends="copy-resources">
        <mkdir dir="${dist.dir}"/>
        <war destfile="${dist.dir}/PetTravelDocs.war" webxml="${build.web.dir}/WEB-INF/web.xml">
            <fileset dir="${build.web.dir}"/>
        </war>
        <echo message="✅ WAR generado: PetTravelDocs.war"/>
    </target>

    <!-- ===========================
         DIST - TAREA PRINCIPAL PARA RENDER
         =========================== -->
    <target name="dist" depends="war">
        <echo message="✅ Build completado para Render"/>
        <echo message="📦 WAR disponible en: ${dist.dir}/PetTravelDocs.war"/>
    </target>

    <!-- ===========================
         CLEAN BUILD
         =========================== -->
    <target name="clean-build" depends="clean,compile,copy-resources,war">
        <echo message="✅ Clean build completado"/>
    </target>

</project>
